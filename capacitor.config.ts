import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Authomation_V1',
  appName: 'Authomation_V1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
